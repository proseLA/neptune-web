export enum ControlType {
  ACCENT = 'accent',
  POSITIVE = 'positive',
  NEGATIVE = 'negative',
}

export enum Priority {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
}
