import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Pagination, PaginationType } from '@transferwise/components/build/es/polyfill/lab';
import { Alert, Sentiment } from '@transferwise/components';
import code from '../../liveEditorCode/lab/pagination.code';

<Alert
  type={Sentiment.WARNING}
  message="Please note that this component is still in a testing phase and we do not guarantee that the API
  will remain the same. Please feel free to test it and provide us a feedback to help improve it
  before the final release."
/>

<LiveEditorBlock code={code} scope={{ Pagination, PaginationType }} display="vertical" />
<GeneratePropsTable componentName="Pagination" />

export const meta = {
  name: 'Pagination',
};
