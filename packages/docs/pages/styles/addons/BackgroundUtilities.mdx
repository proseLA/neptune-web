import { Link } from '@transferwise/components';
import { CssClass } from '../../../components/Css';

export function Demo({ name, comment }) {
  return (
    <div className="m-b-2">
      <div
        className={name}
        style={{
          border: '1px solid var(--color-border-neutral)',
          display: 'inline-flex',
          justifyContent: 'center',
          alignItems: 'center',
          borderRadius: '3px',
          width: '160px',
          height: '48px',
        }}
      >
        <CssClass name={name} />
      </div>{' '}
      {comment}
    </div>
  );
}

import { DocumentLink } from '../../../utils/pageUtils';

Background utility classes.

## Contextual backgrounds

Based on <Link target="_blank" href="https://neptune.wise.com/design/colour/#background">background colors</Link>.

<div className="m-b-4">
  <Demo name="bg-screen" />
  <Demo name="bg-elevated" />
  <Demo
    name="bg-neutral"
    comment={
      <>
        Use to be <CssClass name="bg-default" code />
      </>
    }
  />
  <Demo
    name="bg-accent"
    comment={
      <>
        Use to be <CssClass name="bg-info" code />
      </>
    }
  />
  <Demo name="bg-primary" />
  <Demo
    name="bg-positive"
    comment={
      <>
        Use to be <CssClass name="bg-success" code />
      </>
    }
  />
  <Demo
    name="bg-negative"
    comment={
      <>
        Use to be <CssClass name="bg-negative" code />
      </>
    }
  />
  <Demo name="bg-warning" />
  <Demo name="bg-overlay" />
</div>

export const meta = {
  name: 'Background utilities',
  badge: {
    expiryDate: '2021-11-15',
    type: 'new',
  },
};
