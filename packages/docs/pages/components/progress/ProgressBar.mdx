import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { ProgressBar, Status, Size } from '@transferwise/components';
import code from '../../../liveEditorCode/progressBar.code';

<LiveEditorBlock code={code} scope={{ ProgressBar }} />
<GeneratePropsTable componentName="ProgressBar" />

export const meta = {
  name: 'ProgressBar',
};
