import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { DefinitionList, Layout } from '@transferwise/components';
import code from '../../../liveEditorCode/definitionList.code';

<LiveEditorBlock code={code} scope={{ DefinitionList, Layout }} />
<GeneratePropsTable componentName="DefinitionList" />

export const meta = {
  name: 'DefinitionList',
};
