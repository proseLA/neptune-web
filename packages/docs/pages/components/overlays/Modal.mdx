import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Modal, Button, DateInput, Scroll, Position, Size } from '@transferwise/components';
import code from '../../../liveEditorCode/modal.code';

<LiveEditorBlock code={code} scope={{ Modal, Button, DateInput, Scroll, Position, Size }} />
<GeneratePropsTable componentName="Modal" />

export const meta = {
  name: 'Modal',
};
