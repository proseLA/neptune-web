import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Drawer, Button, Position } from '@transferwise/components';
import code from '../../../liveEditorCode/drawer.code';

<LiveEditorBlock code={code} scope={{ Drawer, Button, Position }} />
<GeneratePropsTable componentName="Drawer" />

export const meta = {
  name: 'Drawer',
};
