import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Button, Popover, Position } from '@transferwise/components';
import code from '../../../liveEditorCode/popover.code';

<LiveEditorBlock code={code} scope={{ Popover, Button, Position }} />
<GeneratePropsTable componentName="Popover" />

export const meta = {
  name: 'Popover',
};
