import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Progress } from '@transferwise/components';
import code from '../../../liveEditorCode/progress.code';

<LiveEditorBlock code={code} scope={{ Progress }} />
<GeneratePropsTable componentName="Progress" />

export const meta = {
  name: 'Progress',
};
