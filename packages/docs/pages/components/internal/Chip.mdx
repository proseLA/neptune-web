import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { DocumentLink } from '../../../utils/pageUtils';
import { Chip } from '@transferwise/components';
import code from '../../../liveEditorCode/chip.code';

See this component used in components such as <DocumentLink href="components/inputs/Typeahead">Typeahead</DocumentLink> and <DocumentLink href="components/inputs/Chips">Chips</DocumentLink>.

<LiveEditorBlock code={code} scope={{ Chip }} />
<GeneratePropsTable componentName="Chip" />

export const meta = {
  name: 'Chip',
};
