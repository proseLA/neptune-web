import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Freeze, Plus, Bin } from '@transferwise/icons';
import { CircularButton, ControlType, Priority } from '@transferwise/components';
import code from '../../../liveEditorCode/circularButton.code';

<LiveEditorBlock code={code} scope={{ CircularButton, ControlType, Freeze }} />
<GeneratePropsTable componentName="CircularButton" />

## Variants

<div className="scroll-table">
  <table className="docs-table table">
    <thead>
      <tr>
        <th>Type</th>
        <th>Priority</th>
        <th>Preview</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Accent</td>
        <td>Primary</td>
        <td>
          <CircularButton
            icon={<Freeze />}
            type={ControlType.ACCENT}
            onClick={() => console.log('Button clicked')}
          >
            Freeze
          </CircularButton>
        </td>
      </tr>
      <tr>
        <td>Accent</td>
        <td>Secondary</td>
        <td>
          <CircularButton
            icon={<Freeze />}
            type={ControlType.ACCENT}
            priority={Priority.SECONDARY}
            onClick={() => console.log('Button clicked')}
          >
            Freeze
          </CircularButton>
        </td>
      </tr>
      <tr>
        <td>Positive</td>
        <td>Primary</td>
        <td>
          <CircularButton
            icon={<Plus />}
            type={ControlType.POSITIVE}
            onClick={() => console.log('Button clicked')}
          >
            Add money
          </CircularButton>
        </td>
      </tr>
      <tr>
        <td>Positive</td>
        <td>Secondary</td>
        <td>
          <CircularButton
            icon={<Plus />}
            type={ControlType.POSITIVE}
            priority={Priority.SECONDARY}
            onClick={() => console.log('Button clicked')}
          >
            Add money
          </CircularButton>
        </td>
      </tr>
      <tr>
        <td>Negative</td>
        <td>Primary</td>
        <td>
          <CircularButton
            icon={<Bin />}
            type={ControlType.NEGATIVE}
            onClick={() => console.log('Button clicked')}
          >
            Delete
          </CircularButton>
        </td>
      </tr>
      <tr>
        <td>Negative</td>
        <td>Secondary</td>
        <td>
          <CircularButton
            icon={<Bin />}
            type={ControlType.NEGATIVE}
            priority={Priority.SECONDARY}
            onClick={() => console.log('Button clicked')}
          >
            Delete
          </CircularButton>
        </td>
      </tr>
    </tbody>
  </table>
</div>

export const meta = {
  name: 'CircularButton',
};
