import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Chips, Link } from '@transferwise/components';
import choiceCode from '../../../liveEditorCode/chips.choice.code';
import filterCode from '../../../liveEditorCode/chips.filter.code';

<span>
  The Chips component can be used for making a <b>choice</b>, or as a <b>filter</b> with multiple
  options. See{' '}
  <Link href="https://neptune.wise.com/components/chip" target="_blank">
    usage guidelines
  </Link>{' '}
  for more info.
</span>

## Choice

The user can only select one chip with the `selected` prop as a single value.

<LiveEditorBlock code={choiceCode} scope={{ Chips }} />

---

## Filter

The user can select any number of chips with the `selected` prop as an array.

<LiveEditorBlock code={filterCode} scope={{ Chips }} />
<GeneratePropsTable componentName="Chips" />

---

export const meta = {
  name: 'Chips',
  badge: {
    type: 'new',
    expiryDate: '2021-11-01',
  },
};
