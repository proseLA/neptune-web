import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { RadioGroup, Avatar, AvatarType } from '@transferwise/components';
import code from '../../../liveEditorCode/radioGroup.code';

<LiveEditorBlock code={code} scope={{ RadioGroup, Avatar, AvatarType }} />
<GeneratePropsTable componentName="RadioGroup" />

export const meta = {
  name: 'RadioGroup',
};
